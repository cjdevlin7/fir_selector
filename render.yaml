services:
  - type: web
    name: fir-selector
    runtime: python
    buildCommand: pip install -r requirements.txt && python3 warm_cartopy_cache.py
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120
    autoDeploy: true
